"""Self-contained, real-LLM full pipeline for the Salão de Ferramentas.

Runs the complete Rules Farmer loop (rule generation + attack variation) for one
attack against local Docker, with a real LLM but without the remote 4-entity
testbed. See pipeline_local/README.md.
"""
