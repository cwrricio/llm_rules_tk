# Copie para .env (na raiz do repo, ao lado de config.yaml) e preencha SOMENTE a
# chave do provedor que você vai usar. As demais podem ficar em branco.
# NUNCA versione o seu .env real (já está no .gitignore).

# LLM providers — preencha uma:
DEEPSEEK_API_KEY=        # provedor usado no artigo (modelo deepseek-v4-pro)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GROQ_API_KEY=
